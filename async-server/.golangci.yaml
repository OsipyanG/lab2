linters:
  enable-all: true
  disable:
    - depguard
    - godox
    - exhaustruct
    - lll
    - gofumpt
    - wsl
linters-settings:
  tagliatelle:
    case:
      rules:
        json: snake
run:
  timeout: 1m
